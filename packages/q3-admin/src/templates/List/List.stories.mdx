import {
  Meta,
  Story,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks';
import Paper from '@material-ui/core/Paper';
import {
  TemplateListCollapse,
  TemplateListMultiColumn,
  TemplateListStack,
} from '../../../__fixtures__/datasource/StoryWrapper';
import { Gallery, MultiColumn, Stack } from '.';

<Meta title="Admin/Templates/List" />

# Detail

## Narrow

<strong>
  Preferred app template: <mark>Collapse</mark>
</strong>

<p>
  The narrow detail template best suits data views with
  limited user actions. Because of its limited width, it
  also pairs nicely with content-heavy applications (i.e.
  blogs). The feature photo only occupies the small space of
  an avatar, so reserve for logos, profile pictures and the
  like.
</p>

<Preview>
  <Story name="Gallery">
    <Paper>
      <TemplateListStack component={Gallery} />
    </Paper>
  </Story>
</Preview>

<Preview>
  <Story name="MultiColumn">
    <Paper>
      <TemplateListCollapse component={MultiColumn} />
    </Paper>
  </Story>
</Preview>

## Split (Legacy)

<strong>
  Preferred app template: <mark>Collapse</mark>
</strong>

<p>
  The default template in Q3 V1 apps is still a good choice
  when the data leverages multiple Q3 features in a
  non-essential way. For instance, the ability to use notes
  and atttach files is a nice-to-have, but it does not fall
  into the workflow otherwise. Mostly, this style suits a
  back-office application.
</p>

<Preview>
  <Story name="Stack (App Stack)">
    <Paper>
      <TemplateListStack component={Stack} />
    </Paper>
  </Story>
  <Story name="Stack (App MultiColumn)">
    <Paper>
      <TemplateListMultiColumn component={Stack} />
    </Paper>
  </Story>
</Preview>
