import {
  Meta,
  Story,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks';
import { Form } from 'q3-ui-forms/lib/builders';
import FilterEntry from './FilterEntry';
import { filterOptions } from '../../__fixtures__/formData';

<Meta title="Admin/FilterEntry" component={FilterEntry} />

# FilterEntry

<Preview>
  <Story name="With">
    <Form debug>
      <FilterEntry
        name="radio"
        type="radio"
        options={filterOptions}
      />
      <FilterEntry
        name="checkset"
        type="checkset"
        options={filterOptions}
      />
      <FilterEntry
        name="chips"
        type="chips"
        options={filterOptions}
      />
    </Form>
  </Story>
</Preview>
