import {
  Meta,
  Story,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks';
import HorizontalNavigation from './HorizontalNavigation';
import Location from 'q3-ui-test-utils/lib/location';
import menuItems, {
  extendedMenuItems,
} from '../../../__fixtures__/menuItems';
import logo from '../../../__fixtures__/logo';

<Meta
  title="Admin/Navigation/HorizontalNavigation"
  component={HorizontalNavigation}
/>

# Horizontal Navigation

<Preview>
  <Story name="Horizontal Navigation">
    <Location initialPath="/savings">
      <Viewport>
        <HorizontalNavigation
          logoSrc={logo}
          menuItems={extendedMenuItems}
          footerComponent={<small>Fine print</small>}
        />
      </Viewport>
    </Location>
  </Story>
</Preview>
