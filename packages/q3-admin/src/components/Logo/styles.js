import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
  link: {
    display: 'block',
    maxHeight: 120,
    height: '100%',
    width: 'auto',
  },
  img: {
    maxHeight: '100%',
    maxWidth: '100%',
  },
}));
