import React from 'react';
import { Box } from '@material-ui/core';
import useStyle from './styles';

const BottomActionSheet = () => (
  <Box
    className={useStyle().root}
    id="q3-mobile-utilities"
  />
);

export default BottomActionSheet;
