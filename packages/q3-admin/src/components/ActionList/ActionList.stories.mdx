import {
  Meta,
  Story,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks';
import IconButton from '@material-ui/core/IconButton';
import KeyboardBackspace from '@material-ui/icons/KeyboardBackspace';
import ActionList from './ActionList';

<Meta title="Admin|Action List" component={ActionList} />

# Action List

This component allows us to dynamically render list actions
for the Import, Export and Report container partials.

<Preview>
  <Story name="With actions">
    <ActionList
      actions={['sampleActionName', 'sampleActionName2']}
      actionTitle="Actions"
    >
      {(template) => (
        <span>{`Action for ${template}`}</span>
      )}
    </ActionList>
  </Story>
</Preview>

<Props of={ActionList} />
