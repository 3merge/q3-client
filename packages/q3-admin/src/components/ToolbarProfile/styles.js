import React from 'react';
import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
  listItem: {
    borderRadius: 8,
    padding: 0,
    paddingRight: '.55rem',
    whiteSpace: 'nowrap',
  },
}));
