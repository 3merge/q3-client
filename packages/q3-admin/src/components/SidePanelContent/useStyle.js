import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  bordered: {
    textOverflow: 'ellipsis',
    position: 'relative',
  },
  subtext: {
    fontSize: '1rem',
  },
}));
