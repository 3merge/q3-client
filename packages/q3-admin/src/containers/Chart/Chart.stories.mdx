import {
  Meta,
  Story,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks';
import Chart from './Chart';
import ChartFixture, {
  FilterComponent,
} from './Chart.fixture';

<Meta title="Admin/Chart" component={Chart} />

# Chart

<Preview>
  <Story name="With custom chart">
    <ChartFixture>
      <Chart
        title="Example bar graph"
        chartComponent={<p>Chart component of prefence</p>}
        filterComponent={<FilterComponent />}
        initialQueryValue="?template=foo"
      />
    </ChartFixture>
  </Story>
</Preview>

<Preview>
  <Story name="With error">
    <ChartFixture>
      <Chart
        title="Example bar graph"
        chartComponent={<p>Chart component of prefence</p>}
        filterComponent={<FilterComponent />}
        initialQueryValue="?s=foo&createdAt<=2012-01-12&createdAt>=2012-09-12"
      />
    </ChartFixture>
  </Story>
</Preview>
