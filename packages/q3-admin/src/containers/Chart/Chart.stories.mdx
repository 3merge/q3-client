import {
  Meta,
  Story,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks';
import Chart from './Chart';
import MockRestAdapter from 'q3-ui-test-utils/lib/rest';
import Location from 'q3-ui-test-utils/lib/location';
import ChartFixture, {
  FilterComponent,
} from './Chart.fixture';
import data from 'q3-ui-charts/tests/fixtures/sample-small';

<Meta title="Admin/Chart" component={Chart} />

# Chart

<Preview>
  <Story name="Bar">
    <Location>
      <MockRestAdapter
        delay={2500}
        define={(m) => {
          m.onGet(/reports\?template=foo/).reply(200, data);
        }}
      >
        <Chart
          component="Bar"
          template="foo"
          title="Example"
        />
      </MockRestAdapter>
    </Location>
  </Story>
</Preview>
