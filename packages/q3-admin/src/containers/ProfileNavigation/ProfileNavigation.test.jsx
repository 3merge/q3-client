import React from 'react';
import ProfileNavigation from './ProfileNavigation';

describe('ProfileNavigation', () => {
  it.todo(
    'should render FeaturedPhoto component  when withPhoto is true',
  );
  it.todo(
    'should not render FeaturedPhoto component when withPhoto is false',
  );
});
