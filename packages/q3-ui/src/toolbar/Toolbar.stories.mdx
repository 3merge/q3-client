import {
  Meta,
  Story,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks';
import Toolbar from '.';
import Header from '../header';

<Meta title="UI/Toolbar" component={Toolbar} />

<Preview>
  <Story name="Toolbar logged in">
    <Toolbar
      isLoggedIn
      loginPath="/login"
      signupPath="/signup"
      name="Gavin"
      profileImgSrc="https://picsum.photos/id/863/200/300"
      items={[
        {
          onClick: () => null,
          label: 'Logout',
        },
        {
          onClick: () => null,
          label: 'Support',
        },
      ]}
    >
      <Translate items={[]} />
    </Toolbar>
  </Story>
</Preview>
