import React from 'react';
import PropTypes from 'prop-types';
import { SelectAll } from 'q3-ui-exports';

export const ColumnSelectAll = ({ ids }) => (
  <SelectAll ids={ids} />
);

export default ColumnSelectAll;
