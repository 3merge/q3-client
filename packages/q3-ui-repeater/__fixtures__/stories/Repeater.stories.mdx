import {
  Meta,
  Story,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks';
import Repeater from '../support';
import rows from '../../../q3-ui-datatables/__fixtures__/orders';
export const isEven = (item) => Number(item.seq) % 2 === 0;
export const isSeq = (item) => Number(item.seq) === 211;

<Meta title="Repeater/Repeater" component={Repeater} />

# Repeater

<Preview>
  <Story name="Empty">
    <Repeater data={[]}>
      <p>Editor</p>
    </Repeater>
  </Story>
  <Story name="Custom renderers">
    <Repeater
      bulkEditorComponent={<p>Bulk editor</p>}
      renderNestedTableRow={() => <p>Check me out</p>}
      perPage={15}
      groupBy={[
        { label: 'Evens', fn: isEven },
        { label: '211', fn: isSeq },
      ]}
      sortOptions={[
        { sortBy: 'seq' },
        { sortBy: 'firstName' },
      ]}
      cardProps={{
        title: 'seq',
        attributes: ['for.name', 'id', 'age'],
        editable: {
          position: {
            renderer: () => <div>Something custom!</div>,
          },
        },
        onColor: (item) =>
          item.age < 20 ? 'orange' : 'blue',
        describe: () =>
          'This is a dynamic sentence generated from the item template.',
        makeLink: () => '/to',
        makeLinkLabel: ({ id }) => id,
      }}
    >
      <p>Editor</p>
    </Repeater>
  </Story>
</Preview>
