import {
  Meta,
  Story,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks';
import Repeater from '../support';
import rows from '../seed/rows';
export const isEven = (x) => x.id % 2 === 0;

<Meta title="Repeater/Test" component={Repeater} />

# Repeater

<Preview>
  <Story name="Empty">
    <Repeater data={[]}>
      <p>Editor</p>
    </Repeater>
  </Story>
  <Story name="Custom renderers">
    <Repeater
      bulkEditorComponent={<p>Bulk editor</p>}
      renderNestedTableRow={() => <p>Check me out</p>}
      perPage={3}
      groupBy={{ label: 'Evens', fn: isEven }}
      sortOptions={[
        { sortBy: 'id' },
        { sortBy: 'firstName' },
      ]}
      cardProps={{
        title: 'firstName',
        attributes: ['firstName', 'id', 'age'],
        editable: {
          position: {
            renderer: () => <div>Something custom!</div>,
          },
        },
        onColor: (item) =>
          item.age < 20 ? 'orange' : 'blue',
        describe: () =>
          'This is a dynamic sentence generated from the item template.',
        makeLink: () => '/to',
        makeLinkLabel: ({ id }) => id,
      }}
    >
      <p>Editor</p>
    </Repeater>
  </Story>
</Preview>
