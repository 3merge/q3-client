import {
  Meta,
  Story,
  Preview,
} from '@storybook/addon-docs/blocks';
import { Container } from '@material-ui/core';
import { Field } from 'q3-ui-forms/lib/builders';
import Comments from './Comments';
import Fixture from '../../tests/fixtures';

<Meta title="Comments" component={Comments} />

<Preview>
  <Story name="Populated">
    <Container style={{ margin: '2rem auto' }}>
      <Fixture>
        <Comments />
      </Fixture>
    </Container>
  </Story>
  <Story name="Loading">
    <Fixture delay={10000}>
      <Comments />
    </Fixture>
  </Story>
  <Story name="Error">
    <Fixture error>
      <Comments />
    </Fixture>
  </Story>
  <Story name="Additional fields">
    <Fixture>
      <Comments
        additionalFields={<Field name="testin" />}
        insertNode={(a, b) => {
          console.log(a, b);
          return 'ADD STUFF';
        }}
      />
    </Fixture>
  </Story>
</Preview>
