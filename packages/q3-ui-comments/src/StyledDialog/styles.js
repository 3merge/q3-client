import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  scrollPaper: {
    alignItems: 'flex-end',
    justifyContent: 'flex-end',
  },
  paper: {
    maxHeight: 450,
  },
}));
