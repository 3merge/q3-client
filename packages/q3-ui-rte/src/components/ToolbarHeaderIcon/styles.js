import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
  root: {
    bottom: '4px',
    fontFamily: 'monospace',
    fontWeight: 'bold',
    fontSize: '10px',
    position: 'absolute',
    right: '8px',
  },
}));
