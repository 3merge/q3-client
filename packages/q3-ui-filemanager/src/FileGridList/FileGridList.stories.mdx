import {
  Meta,
  Story,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks';
import FileGridList from './FileGridList';
import files from '../../tests/fixtures/files.json';
import support from '../../tests/support';

<Meta title="Files/FileGridList" component={FileGridList} />

# FileGridList

<Preview>
  <Story name="As default">
    <FileGridList
      files={files}
      onDrop={support.onDrop.succeed}
      onDelete={() => {}}
    />
  </Story>
</Preview>

## API

<Props of={FileGridList} />
