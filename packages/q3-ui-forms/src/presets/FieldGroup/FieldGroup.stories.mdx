import {
  Meta,
  Story,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks';
import FieldGroup from '.';
import { Form, Field } from '../../builders';

<Meta
  title="Forms/Presets/FieldGroup"
  component={FieldGroup}
/>

# FieldGroup

<Preview>
  <Story name="Types">
    <LocationDebugger />
    <Form debug>
      <FieldGroup label="Custom label">
        <Field
          name="field"
          helper="Type 'appear' to make the next fieldgroup show"
          xl={12}
          lg={12}
        />
      </FieldGroup>
      <FieldGroup
        conditional={['field=appear']}
        label="Hidden"
      >
        <Field name="hidden" xl={12} lg={12} />
      </FieldGroup>
    </Form>
  </Story>
</Preview>
