import {
  Meta,
  Story,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks';
import Password from './NewPasswordFields';
import { Form, Field } from '../../builders';

<Meta
  title="Forms/Presets/NewPassword"
  component={Password}
/>

<Preview>
  <Story name="New Password">
    <Form debug>
      {(values) => <Password {...values} />}
    </Form>
  </Story>
</Preview>
